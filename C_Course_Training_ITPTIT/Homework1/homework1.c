#include<stdio.h>
#include<stdlib.h>
#include<math.h>

int main()
{
    //Nhập xuất
    /*int a;
    long long b;
    char kytu;
    float c;
    double d;
    scanf("%d %lld %c %f %lf", &a, &b, &kytu, &c, &d);
    printf("%d\n\n%lld\n\n%c\n\n%.3f\n\n%.7lf", a, b, kytu, c, d);*/

    //Tính thể tích hình chóp
    /*float h;
    float r;
    scanf("%f %f", &h, &r);
    double v;
    v = 3.14 * r * r * h * 1/3;
    printf("%.3lf", v);*/

    //Tính tuổi
    /*int x;
    scanf("%d", &x);
    int tuoi = 2019 - x;
    printf("%d", tuoi);*/


    //Tính tổng dãy số (Tổng 1)
    /*long long n;
    scanf("%lld",&n);
    long long a;
    a = ((((n - 1)/2)+1)*(n+1))/2;
    printf("%lld", a);*/

    //MinMax
    /*long long a,b,c,x,y,z,min,max;
    scanf("%lld %lld %lld", &a, &b, &c);
    x = (a + b + abs(a - b)) / 2;
    y = (a + b - abs(a - b)) / 2;
    max = (x + c + abs(x - c)) / 2;
    min = (y + c - abs(y - c)) / 2;

    printf("%lld %lld", max, min);*/


    //Tỉnh giấc
    // double a,b,so;
    // long long x,y,z,m,n,sum;
    // scanf("%lf %lf", &a, &b);
    // so = (a / b) * 100000;
    // long long nhan = so;
    // x = nhan % 10;
    // y = (((nhan - x) / 10) % 10);
    // z = (((((nhan - x) / 10) - y) / 10) % 10);
    // m = (((((((nhan - x) / 10) - y) / 10) - z) / 10) % 10); 
    // n = (((((((((nhan - x) / 10) - y) / 10) - z) / 10) - m) / 10) % 10);
    // sum = x + y + z + m + n;
    // printf("%lld", sum);

    

    //Tổng 2
    /*long long n,tong;
    scanf("%lld", &n);
    tong = (n * (n + 1) * ((2 * n) + 1 )) / 6;
    printf("%lld", tong);*/


    //Tổng dư
    // long long a,N,tong;
    // scanf("%lld %lld", &a, &N);  
    // tong = ((N - 1) * N * (a / N) / 2) + ((a % N + 1) * (a % N) / 2);
    // if (tong == N)
    // {
    //     /* code */
    //     printf("1");
    // }
    // else
    // {
    //     /* code */
    //     printf("0");
    // }


    //Ốc sên
    // long long a,b,V,time;
    // scanf("%lld %lld %ldd", &a, &b, &V);
    // if (a == V)
    // {
    //     /* code */
    //     printf("1");
    // }
    // if ((V - b) % (a - b) == 0)
    // {
    //     /* code */
    //     time = ((V - b) / (a - b));
    // }
    // else
    // {
    //     /* code */
    //     time = ((V - b) / (a - b)) + 1;
    // }
    // printf("%lld", time);

    //Tổng 1
    // long long a,b,c,d,e,f,g;
    // scanf("%lld" "%lld" "%lld",&a,&b,&c);
    // d = ((a + b) + abs(a - b))/2;
    // e = ((a + b) - abs(a - b))/2;
    // f = ((d + e) + abs(d - e))/2;
    // g = ((d + e) - abs(d - e))/2;
    // printf("%lld",f);
    // printf("%lld",g);
	// return 0;

}    
