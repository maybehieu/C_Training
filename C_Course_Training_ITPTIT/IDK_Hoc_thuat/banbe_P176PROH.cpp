#include <bits/stdc++.h>
using namespace std;

int main()
{
    int bo;
    cin >> bo;
    while (bo--)
    {
        long long x1, x2, x3;
        cin >> x1 >> x2 >> x3;
        int max = -1;
        if (abs(x1) > abs(x2))
        {
            /* code */
        }
        }

    return 0;
}