#include <bits/stdc++.h>
using namespace std;
int arr[1000000];
int main()
{
    int bo;
    scanf("%d", &bo);
    while (bo--)
    {
        int n, sum;
        scanf("%d %d", &n, &sum);
        for (int i = 0; i < n; i++)
        {
            scanf("%d", &arr[i]);
        }
        for (int i = 0; i < n; i++)
        {
        }
    }

    return 0;
}