#include <bits/stdc++.h>
using namespace std;

int main()
{
    int bo;
    scanf("%d", &bo);
    while (bo--)
    {
        int n,so;
        scanf("%d %d", &n, &so);
        
    }
    
    return 0;
}
